# Test Report — RED — {{feature-slug}}

> Produced by `tdd-specialist` in Phase 3. Confirms that all new tests exist and fail before any implementation. Generated by `./.agent/scripts/run-tests.sh red` after test files are written.

## Run Metadata

- **Mission ID:** `{{MISSION_ID}}`
- **Ran at:** `{{ISO_8601_TIMESTAMP}}`
- **Command:** `php artisan test --testsuite=Feature,Unit`
- **Agent + Model:** `tdd-specialist` · `{tier} → {model-id resolved from .agent/config.json → tier_mapping}`
- **PHP version:** `{{output of `php -v` first line}}`
- **Laravel version:** `{{output of `php artisan --version`}}`

## Tests Created

Every test written in this phase, mapped back to the Testing Contract in `DESIGN.md`. If any row is missing, the design contract is not satisfied.

| File | Class :: Method | Covers (from DESIGN) | Expected Failure Reason |
|---|---|---|---|
| `tests/Feature/{{File}}.php` | `{{Class}}::{{method}}` | `{{DESIGN §}}` | Class/route not yet implemented |
| `tests/Unit/Actions/{{File}}.php` | `{{Class}}::{{method}}` | `{{DESIGN §}}` | Action not yet implemented |

## Full Test Output

```
{{paste the full `php artisan test` output here — do not truncate}}
```

## Failure Summary

Each new test must be in the FAIL column. Any accidental PASS is a red flag — either the test is wrong or code already exists that shouldn't.

- **Total new tests:** `{{N}}`
- **Failing as expected:** `{{N}}`
- **Passing unexpectedly:** `{{0 required — if > 0, list them and stop}}`
- **Errors (compile/typo):** `{{0 required — if > 0, fix before proceeding}}`

## Pre-existing Suite Status

Running all tests must not regress anything that was green before.

- **Pre-existing tests before this mission:** `{{N}}`
- **Pre-existing tests still passing:** `{{N}}`
- **Pre-existing tests broken by new code:** `{{0 required}}`

## Hand-off to Developer

- Tests live in: `{{paths}}`
- DESIGN.md sections to follow: `{{reference §}}`
- Files the developer must create: see `DESIGN.md → File Paths Contract`.
