# Final Review — {{feature-slug}}

> Produced jointly in Phase 5: `tdd-specialist` fills the Green section; `architect` fills the Drift Review. Must be approved by the user before Phase 6 (closure).

## Run Metadata

- **Mission ID:** `{{MISSION_ID}}`
- **Reviewed at:** `{{ISO_8601_TIMESTAMP}}`
- **Original Scope:** `{{original_user_prompt}}`

---

## Part A — Green Verification (`tdd-specialist`)

Generated by `./.agent/scripts/run-tests.sh green`.

### Test Results

- **Command:** `php artisan test`
- **Total tests:** `{{N}}`
- **Passing:** `{{N}} (must equal total)`
- **Failing:** `{{0 required}}`
- **Skipped:** `{{N}}`
- **Duration:** `{{seconds}}`

### Coverage vs. DESIGN Contract

Every test listed in `DESIGN.md → Testing Contract` must exist and pass.

| DESIGN § | Test                | Status  |
| -------- | ------------------- | ------- |
| {{§}}    | `{{Class::method}}` | ✅ pass |

### Full Test Output

```
{{paste full output — do not truncate}}
```

---

## Part B — Architectural Drift Review (`architect`)

Compare what was built against `DESIGN.md`. Drift is not always wrong — but it must be surfaced.

### File Paths Audit

Compare `git diff --name-only` against `DESIGN.md → File Paths Contract`.

| File                         | In DESIGN? | Created? | Notes                         |
| ---------------------------- | ---------- | -------- | ----------------------------- |
| `app/Actions/{{Action}}.php` | ✅         | ✅       | matches                       |
| `{{unexpected file}}`        | ❌         | ✅       | **drift — justify or remove** |

- **Expected files not created:** `{{list or "none"}}`
- **Unexpected files created:** `{{list or "none"}}`

### Pattern Compliance

- **Controllers contain no logic (Actions only):** `{{yes/no + evidence}}`
- **Typed properties used (PHP 8.2+):** `{{yes/no}}`
- **Inertia props match TypeScript interfaces:** `{{yes/no}}`
- **Tailwind v4 — no ad-hoc CSS introduced:** `{{yes/no}}`
- **No Repository Pattern introduced unless justified in DESIGN:** `{{yes/no}}`

### Drift Items

For each deviation from DESIGN.md, decide: accept, remediate, or defer.

| #   | Deviation                                             | Decision                       | Rationale        |
| --- | ----------------------------------------------------- | ------------------------------ | ---------------- |
| 1   | {{e.g. "Added a `Notification` trait not in DESIGN"}} | {{accept / remediate / defer}} | {{one sentence}} |

### Verdict

- [ ] ✅ Approve — ready for user review.
- [ ] 🔁 Send back to Phase 4 — implementation drifted; developer must remediate items above.
- [ ] 🔁 Send back to Phase 3 — tests did not cover DESIGN contract.
- [ ] 🔁 Send back to Phase 2 — DESIGN itself was wrong; needs revision.

---

### 📊 Mission Telemetry (Workspace Estimate)

Must compile the Telemetry data available

| Metric                       | Value               |
| :--------------------------- | :------------------ |
| **Agent**                    | agent1, agent2, etc |
| **Model**                    | a, b, c.            |
| **Files Read**               | X                   |
| **Files Modified**           | X                   |
| **Total Turns**              | X                   |
| **Est. Context Tokens**      | ~X                  |
| **Est. Generated Tokens**    | ~X                  |
| **Est. API Equivalent Cost** | **$ XX **           |

---

## Hand-off to User

Summary in 3 bullets or fewer. This is what the user sees at the gate.

- {{bullet 1}}
- {{bullet 2}}
- {{bullet 3}}
